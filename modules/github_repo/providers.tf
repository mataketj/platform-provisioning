terraform {
  required_providers {
    github = {
      source  = "integrations/github"
      version = "~> 6.0"
    }
  }
}

provider "github" {
  # Authentication is typically handled via GITHUB_TOKEN environment variable
}
